server:
  port: 8089

spring:
  application:
    name: enricher-service

management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"
  metrics:
    tags:
      application: enricher-service

enricher:
  search-service:
    base-url: http://localhost:8081
    global-endpoint: /api/objects/{objectClass}?globalId={globalId}
    revision-endpoint: /api/objects/{objectClass}/revisions/{id}
    parent-endpoint: /api/objects/{objectClass}/parent/{parentId}
    global-item-endpoint: /api/v2/objects/global-item?relationGlobalId={relationGlobalId}&idFieldName={idFieldName}&roleFieldName={roleFieldName}&role={role}
  metadata:
    url: http://localhost:8083/api/search-service/metadata/v2
    local-file: ""
